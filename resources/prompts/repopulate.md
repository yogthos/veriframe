[harness] The beam has fallen below the width this run asked for, and you are
the strongest line still running — so you are the one that can refill it.

Name the distinct directions still worth exploring and call `branch_theses`.
The first commits you and the rest become siblings, each opening with what
every lineage here has already confirmed, so a new line starts from the run's
evidence rather than from nothing.

Distinct is the whole point. Two theses that differ only in wording buy no
coverage and cost a process and a model call per turn each. Look for
directions that would fail for *different* reasons: a different mechanism, a
different engine, a different thing to be wrong about.

If you genuinely see only one route left, say so by continuing — that is a
real answer. But a beam at width one is exploring nothing in parallel, and
whatever killed the other lines is now the only thing standing between this
run and no answer at all.
