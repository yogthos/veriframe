The last few verifications have not moved you forward — they failed, errored, or restated a claim you already tried. Step back before writing more code:

- **Is the claim too ambitious for one step?** Decompose it. One fact per claim.
- **Did a recent rule break things?** `retract_rule` undoes a tentative named `add_rule`.
- **Is the encoding shape wrong?** Form a different hypothesis rather than patching this one. Repairing a broken encoding usually produces a differently broken encoding.
