{"id":"int-eeb94599","kind":"field_change","created_at":"2026-08-05T13:06:52.121554Z","actor":"Yogthos","issue_id":"veriframe-clj-5dn","extra":{"field":"priority","new_value":"3","old_value":"2"}}
{"id":"int-d0ac58ef","kind":"field_change","created_at":"2026-08-05T13:37:57.265646Z","actor":"Yogthos","issue_id":"veriframe-clj-6v9","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed upstream in jolt-lang/http-client cc45d7e. Root cause was not that a blocking FFI read is unbounded in principle, but that connect-stream took a read-timeout and applied it only on the plaintext branch — tls-connect opened its socket without SO_RCVTIMEO, so :socket-timeout had no effect on any https request and every provider call is https. Now applied. Against a TLS server that completes the handshake and then never answers, a 2000ms socket-timeout gives up at 2074ms with a SocketTimeoutException; before the fix the same request ran past 45 minutes. Regression test: jolt.http.timeout-test/https-honours-socket-timeout, under the :timeouttest alias.\n\nThe scheduler mitigation this bead designed (pipeline instead of barrier) is NOT needed and was not implemented — the barrier was only fatal because one branch could park forever, which it no longer can.\n\nTwo residuals, both deliberately left: :conn-timeout is still ignored, because setting O_NONBLOCK needs variadic fcntl and Apple arm64 passes variadic args on the stack, so a fixed-arity binding silently corrupts them (F_SETFL with 6 read back as 194, O_NONBLOCK unset) — connect is at least kernel-bounded by the SYN retry limit, unlike recv. And SO_RCVTIMEO bounds inactivity rather than total duration, filed separately."}}
{"id":"int-1f634bb5","kind":"field_change","created_at":"2026-08-05T13:38:17.865765Z","actor":"Yogthos","issue_id":"veriframe-clj-9pl","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed upstream in jolt-lang/http-client cc45d7e, and the four-way characterization in this bead turned out to be the tell. Chez resolves a foreign symbol against loaded shared objects most-recent-first, and defcfn resolves when the def is evaluated, so whichever object loaded last before jolt.http.tls decides who owns SSL_*. On macOS the process image transitively links /usr/lib/libssl.dylib, which is LibreSSL. jolt.nrepl calls (ffi/load-library) with no argument at ns load to bind sockets, which loads the process's own symbols and therefore puts LibreSSL ahead of the OpenSSL that :jolt/native loaded.\n\nThe failure was not a missing symbol but a silent mix: TLS_client_method from one implementation and SSL_CTX_new from the other, whose SSL_CTX layouts disagree, so the first call through it died with 'invalid memory reference'. OpenSSL_version confirmed it directly — OpenSSL 3.6.2 without the nrepl require, LibreSSL 3.3.6 with it.\n\nThat also explains why a prior SUCCESSFUL https call immunized the process and a hand-built SSL_CTX did not: what mattered was whether the tls bindings had already resolved, not any ongoing state.\n\njolt.http.tls now loads libcrypto and then libssl itself immediately before its bindings resolve, so load order no longer decides. veriframe.core drops the workaround: jolt.nrepl is a plain static require in the ns form again, which is also what gets nREPL into a built binary. smoke/nrepl-load-order-check still passes."}}
{"id":"int-7054ef2f","kind":"field_change","created_at":"2026-08-05T13:38:31.56802Z","actor":"Yogthos","issue_id":"veriframe-clj-sqf","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed upstream in jolt-lang/http-client cc45d7e, same root cause as veriframe-clj-9pl. The guess recorded here was right: the OpenSSL declared under :jolt/native was not reaching the built image, so nothing loaded it and the SSL_* symbols fell through to the LibreSSL the macOS process image links. Not a distinct AOT problem — the same symbol-resolution hazard reached by a different route, which is why one fix closed both.\n\njolt.http.tls now loads libcrypto and libssl itself at ns init, which runs in a built image too.\n\nVerified end to end on a jolt build binary: it completes a TLS handshake, validates the provider at boot ('provider :deepseek reachable, deepseek-v4-flash available'), starts nREPL, and ran a full verified completion — 2 branches, 20 turns, 6 confirmed artifacts across Z3 and Prolog, shipping x=9 y=4 for 3x+5y=47 with x>y. So the binary is no longer limited to engine paths and a local plain-http provider.\n\nnREPL now starts in a binary as well. That was never a separate defect: it only failed because the workaround for 9pl needed a runtime require, which jolt build cannot reach statically. Removing the workaround removed it.\n\nThe static-reachability constraint itself still stands and the CI build job still guards it."}}
{"id":"int-8d72ff91","kind":"field_change","created_at":"2026-08-05T14:46:29.334113Z","actor":"Yogthos","issue_id":"veriframe-clj-iud","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Fixed. All 17 dispatched tools are now documented in resources/prompts/system.md, organized by engine, plus a 'Choosing an engine' section so the model picks by problem shape rather than reading a flat menu, and a note on which tools reach the slow tier (verify_template, or a Lean proof closed with proof_step) — which the tier-escalation gate had been pushing branches toward without ever saying how to get there.\n\nAlso fixed the quieter instance of the same bug: verify_template's four templates were only listed in the error returned after guessing a wrong name, so a template the model never guessed was invisible. loop/system-prompt now substitutes the catalogue from smt-templates/list-templates, since it is pure data.\n\nDrift is now guarded in both directions by veriframe.prompt-test: every name in tools/tool-names must appear in the prompt, and the prompt must name nothing run-tool cannot dispatch. Both were verified against a deliberately broken prompt rather than assumed — deleting proof_state and adding a fictional verify_coq each fail with the right message. Suite is 56 tests / 339 assertions green.\n\nVerified live: on a non-finite problem (sum of first n odds = n^2), both branches reached for Lean unprompted — B1 called verify_lean, B2 called lean_search, which returned real Mathlib hits. Neither tool was reachable before.\n\nThat run also exposed a defect this bead was hiding: verify_lean fails because the Mathlib import exceeds its timeout. Filed separately — it was invisible for as long as nothing could invoke the engine."}}
{"id":"int-61b93b5f","kind":"field_change","created_at":"2026-08-05T16:03:53.602528Z","actor":"Yogthos","issue_id":"veriframe-clj-j3j","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed on three fronts, all measured rather than estimated.\n\n**The timeout was one knob doing two jobs.** `:engines :lean :timeout-ms` covered both `import Mathlib` and every subsequent command, and those want opposite values. Split into `:timeout-ms` (per command, 300000) and `:import-timeout-ms` (1200000), with `send-command` taking a per-call override so only the import gets the generous ceiling. `HARNESS_LEAN_IMPORT_TIMEOUT_MS` exposes it.\n\n**Warming moved the import out of the turn.** veriframe.engine.lean-pool warms sessions at startup on background threads, so `system/start!` returns immediately and the server answers /health while the import runs. A branch checks out a warmed session; if one is still importing it waits on that rather than starting a second beside it, and if the pool is empty or a warm failed it builds its own, so this is never a dependency. `system/stop!` disposes them.\n\n**Other timeouts raised.** z3 and swipl 30000 to 120000, turn deadline 420000 to 900000. The deadline is now sized to the worst LEGITIMATE turn — a provider call at its 300000ms socket timeout plus a Lean tactic at its own — because forfeiting a turn that was about to succeed costs the branch the turn AND logs a failure other branches then avoid retrying.\n\n## Measurements\n\nImport cost, idle machine, warm olean cache: 377927ms, then 384334ms and 375026ms on a repeat run. Warmed at startup in the running harness: 354490ms.\n\nA second import in the same process is NOT cheaper (384334 then 375026, a 2.4% difference that is noise). So the cost is CPU-bound elaboration, not disk, and the OS page cache does not help. That matters for sizing: one warm session helps exactly one branch, and a beam that genuinely uses Lean wants `HARNESS_LEAN_WARM_SESSIONS` equal to its width. A warmed session measures 0.83GB RSS, so 5 is about 4.2GB — the default stays 1 because most problems never touch Lean.\n\n## Verified end to end\n\nBooted with warming on: /health reported `{installed: true, warm_sessions: 0, warming: 1}` within 30 seconds, then `warm_sessions: 1` after 354516ms. A Lean problem then confirmed `verify_lean` ON TURN 1, and warm_sessions dropped 1 to 0 showing the checkout. Three confirmed Lean artifacts in the run. Before this change the first Lean call always died at the 300000ms import timeout.\n\n## Also fixed here\n\nThe false assurances this bead named. /health now reports `lean.warm_sessions` and `lean.warming` next to `engines.lean`, because installed and ready are different things and conflating them is how the health check stayed green while every Lean call failed. smoke/lean-check was `(.exists (File. repl-bin))`; it now actually imports Mathlib and elaborates a theorem, which makes the probe slow and able to fail the way the engine fails.\n\nSuite 63 tests / 356 assertions green, including veriframe.pool-test covering the checkout bookkeeping offline — no session issued twice under concurrency, a still-importing slot returned to the pool rather than leaked, a failed warm falling through to nil.\n\nStill open and filed separately: sharing one Mathlib environment across branches instead of one session each, which is the structural fix the per-branch cost argues for."}}
{"id":"int-99c83775","kind":"field_change","created_at":"2026-08-05T16:56:18.454169Z","actor":"Yogthos","issue_id":"veriframe-clj-vr4","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Resolved, and the resolution reversed the licence decision that prompted it.\n\nTwo corrections to what this bead claimed. It said all eight jolt-lang repositories were unlicensed; two were not, and my check was wrong (`gh repo view --json licenseInfo` reported NONE across the board, while the REST `.license.spdx_id` gives the real answer). jolt was already EPL-2.0 and logging EPL-1.0. It also implied this needed an upstream conversation; there is admin access on every one of those repositories, so it did not.\n\nSix libraries genuinely had no licence and now have EPL-2.0, matching jolt and Clojure convention: http-client, db, jolt-crypto, nrepl, time, ring-chez-adapter. All eight now report a licence.\n\n`clj-commons/clj-http-lite` also reported as unlicensed and is not. It is MIT, declared in its pom.xml and its README, with no LICENSE file for GitHub's detector to find. Nothing to do, and the claim in this bead was wrong.\n\nThe larger finding was that GPLv3 was never viable. jolt is EPL-2.0 with the secondary-licence option NOT exercised (its Exhibit A is the unfilled template, and the licence text says including Exhibit A alone is not sufficient), and data.json, tools.logging and jolt-lang/logging are EPL-1.0 by inheritance from Clojure code that is not ours to relicense. The FSF lists EPL as GPL-incompatible in both versions. So a GPLv3 veriframe could not legally link its own platform, whatever happened to the unlicensed libraries.\n\nveriframe is EPL-2.0 as of this change: LICENSE replaced, 51 file headers converted, README rewritten with the reasoning. The only thing that had required GPLv3 was embedding Octave in-process, and Octave moves to a subprocess like z3 and swipl, where its GPL does not reach across the process boundary. veriframe-clj-15u carries that change.\n\nEvery dependency is now compatible with EPL-2.0: EPL-2.0 for the jolt-lang libraries, EPL-1.0 for the Clojure-derived ones, MIT for clj-http-lite."}}
{"id":"int-e0838435","kind":"field_change","created_at":"2026-08-05T18:32:11.811875Z","actor":"Yogthos","issue_id":"veriframe-clj-qm7","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Fixed, and the cause was not in this project's process handling. It was that no engine call was ever bounded at all.\n\njolt's clojure.core/deref invokes a record's `deref` method with only the value itself and drops any opts, so the timed three-arity (deref ref timeout-ms timeout-value) silently becomes the blocking one-arity for a record implementing IBlockingDeref. babashka.process implements it exactly that way. Measured directly: (deref proc 2000 ::timeout) on a `sleep 60` waited the full sixty seconds and returned the real result rather than ::timeout.\n\nSo proc/run never saw ::timeout, destroy-tree never ran, and the timeout branch was unreachable code. The orphans were branches abandoned at their deadline while proc/run was still blocked, leaving the subprocess with nobody to reap it. Every hypothesis in the original description was aimed at the reaping and none of them was it.\n\nIt fails in the quietest possible direction: no error, no warning, and the returned value is the correct result of a call that merely took longer than the caller allowed.\n\nproc/run now waits with .waitFor and an explicit timeout, which the host implements correctly (it polls at 10ms and returns false). Reaping escalates SIGTERM to SIGKILL rather than stopping at destroy-tree, which sends only SIGTERM, on the grounds the rest of this loop already applies: a process being killed for ignoring its deadline is exactly the one that may ignore a polite signal.\n\nVerified. Three 2000ms timeouts against `sleep 60` return in about 2.8s each with nothing left behind. A live two-branch run exercising verify_smt and verify_template completed with z3, swipl and repl counts all zero afterwards. Regression tests in engine-test, checked against the old implementation rather than assumed.\n\nUpstream fix opened as jolt-lang/jolt#537 with make unit passing and make cts matching baseline. Nothing here waits on it.\n\nWorth recording what this cost while it was unfixed, because it was more than wasted CPU. The accumulated orphans held up to 450MB each on a 16GB machine already in swap, which made Lean's mmapped oleans re-fault on every import and put `import Mathlib` at ~377s instead of ~23s. That drove an entire investigation into Lean startup cost, a warm-session pool, an import timeout sized to the wrong number, and sizing advice about warm sessions per branch that does not hold. See the corrections on veriframe-clj-j3j. Any benchmark timing taken before this fix should be treated as suspect."}}
{"id":"int-9c6e09e4","kind":"field_change","created_at":"2026-08-05T19:56:35.374742Z","actor":"Yogthos","issue_id":"veriframe-clj-dpj","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Not building it. Measured first, and the contention this was written to remove is about 0.027% of a branch turn.\n\n## Measurements\n\nAgainst the real store, EPL-2.0 tree, 2026-08-05:\n\n    journal write, single thread      median 2.49ms   max 11.2ms\n    FTS read, uncontended             median 0.23ms   max  0.53ms\n    FTS read, 5 concurrent writers    median 2.45ms   max  6.9ms\n    FTS read, 12 concurrent writers   median 0.74ms   max 18.6ms\n\n12 writers is the shape this bead cites (4 problems x 3 branches in the sweep). The read slowdown under contention is real and it is also sub-millisecond to low-millisecond.\n\nFrom an actual completed Lean run: 25 turns produced 89 rows in total across turns, events, artifacts and failures, so about 3.6 writes per turn, and the average wall-clock gap between consecutive turns was 37453ms. That puts DB work at roughly 10ms in a 37-second turn, because the turn is dominated by a provider call and an engine call. Over a full run at beam width 5 and 80 turns the store accounts for a few seconds out of hours.\n\n## Why the trade is bad, not merely unnecessary\n\nThe saving is unobservable. The costs are not:\n\nDurability. Buffered rows are lost on a hard kill. That contradicts the property this system leads with -- everything appended as it happens so a crashed or aborted run stays fully inspectable -- and it is the property that lets a UI be a client rather than a special case. Trading it for 10ms is trading the thing for a rounding error on the thing it costs.\n\nA silent failure mode. If the drainer thread dies the journal stops while the run looks healthy. This bead names that risk itself and compares it to the progress-stalled gate that referenced a missing prompt file and had never once worked. Adding a second mechanism with that shape, to save nothing measurable, is not a good exchange.\n\nCoordination. flush! at run end, on abort, and in the journal-tail endpoint before it serves, each a place to get it wrong.\n\n## The cheap alternative, also declined\n\nWAL with synchronous=NORMAL was measured: write median 2.49ms to 1.73ms, but the tail worsens from 11ms to 53ms as checkpoints land, and reads under 12 writers were unchanged at ~0.7ms. WAL's real benefit is readers running concurrently with a writer, which the single conn-lock forecloses anyway. Not worth the change on these numbers; revisit if the lock ever comes off.\n\n## When to reopen\n\nIf the store shows up in a profile. Concretely: if DB time exceeds a few percent of turn time, or if turn duration drops toward the tens of milliseconds where 10ms would matter. Neither is plausible while a provider call is in the loop.\n\nThe one thing that WOULD change this is dropping the global lock, since the lock is what makes reads wait behind writes at all. That is its own piece of work and would need the concurrent-prepare crash solved another way -- a second read-only connection is the obvious route, and it does not need a queue."}}
{"id":"int-09ff290f","kind":"field_change","created_at":"2026-08-05T20:10:39.242051Z","actor":"Yogthos","issue_id":"veriframe-clj-479","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Won't do. Subprocesses work reliably and the isolation they provide is worth more than anything FFI was going to buy.\n\nThe evidence accumulated against this from several directions rather than one:\n\nIsolation is a real feature, not just a fallback. Each branch gets its own process per engine, so a runaway or crashing engine costs one branch instead of the run. The input is model-generated and adversarial -- the linters exist because models emit encodings that are wrong in surprising ways -- which is exactly the case where a process boundary earns its keep. In-process, all branches share one heap and a segfault takes everything.\n\nThe FFI components produced this project's worst bugs, and all three were the same shape: a failure that is silent or a fault rather than an error. jolt-lang/db gave an invalid memory reference that failed all fourteen benchmark problems, from concurrent sqlite3_prepare_v2 on a shared handle. jolt-lang/http-client dropped the read timeout on the https path, so every provider call was unbounded. And the OpenSSL symbol-resolution hazard produced not a link error but a silent mix of LibreSSL and OpenSSL whose struct layouts disagreed, faulting on first use. A subprocess that misbehaves returns a bad exit code or hangs, and both are catchable.\n\nThe licence forecloses part of it anyway. veriframe is EPL-2.0 because jolt is EPL-2.0 without the secondary-licence option and three more dependencies are EPL-1.0 by inheritance from Clojure code. Linking a GPL engine in-process would force GPLv3, which is incompatible with that. Octave is out on those grounds regardless of engineering merit.\n\nThe per-engine case was weak even before that. SWI-Prolog's session.pl already returns structured JSON via library(http/json), so there is no text parsing for FFI to eliminate -- the current design already achieves what the epic was for. Lean's repl is a Lean program, so converting means reimplementing its frontend against a C API aimed at embedding the runtime. Z3 was the only clear win and it was gated on a P0 that has since been fixed by other means.\n\nAnd the thing that made subprocess bounding look unreliable was not the subprocess. proc/run's timeout branch was unreachable because jolt's deref dropped opts for records implementing IBlockingDeref, so no engine call was ever bounded. Fixed in proc/run and upstream as jolt-lang/jolt#537. Subprocesses are killable, unconditionally, which is the property the RAX-manager principle this loop follows actually asks for: the stop path must not depend on the component's cooperation. A hung PL_next_solution is not killable; a hung swipl is.\n\nReopen only if a specific engine shows a measured cost that a process boundary is demonstrably causing. Note that the last two things attributed to subprocess overhead both turned out to be something else: the Mathlib import cost was orphaned z3 processes, and the store contention was 0.027% of a turn.\n\nveriframe-clj-15u stays open and is detached from this. It is now a subprocess engine like the other three and no longer depends on any of this."}}
{"id":"int-c8fc800d","kind":"field_change","created_at":"2026-08-05T20:10:39.518739Z","actor":"Yogthos","issue_id":"veriframe-clj-aj7","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Won't do. Subprocesses work reliably and the isolation they provide is worth more than anything FFI was going to buy.\n\nThe evidence accumulated against this from several directions rather than one:\n\nIsolation is a real feature, not just a fallback. Each branch gets its own process per engine, so a runaway or crashing engine costs one branch instead of the run. The input is model-generated and adversarial -- the linters exist because models emit encodings that are wrong in surprising ways -- which is exactly the case where a process boundary earns its keep. In-process, all branches share one heap and a segfault takes everything.\n\nThe FFI components produced this project's worst bugs, and all three were the same shape: a failure that is silent or a fault rather than an error. jolt-lang/db gave an invalid memory reference that failed all fourteen benchmark problems, from concurrent sqlite3_prepare_v2 on a shared handle. jolt-lang/http-client dropped the read timeout on the https path, so every provider call was unbounded. And the OpenSSL symbol-resolution hazard produced not a link error but a silent mix of LibreSSL and OpenSSL whose struct layouts disagreed, faulting on first use. A subprocess that misbehaves returns a bad exit code or hangs, and both are catchable.\n\nThe licence forecloses part of it anyway. veriframe is EPL-2.0 because jolt is EPL-2.0 without the secondary-licence option and three more dependencies are EPL-1.0 by inheritance from Clojure code. Linking a GPL engine in-process would force GPLv3, which is incompatible with that. Octave is out on those grounds regardless of engineering merit.\n\nThe per-engine case was weak even before that. SWI-Prolog's session.pl already returns structured JSON via library(http/json), so there is no text parsing for FFI to eliminate -- the current design already achieves what the epic was for. Lean's repl is a Lean program, so converting means reimplementing its frontend against a C API aimed at embedding the runtime. Z3 was the only clear win and it was gated on a P0 that has since been fixed by other means.\n\nAnd the thing that made subprocess bounding look unreliable was not the subprocess. proc/run's timeout branch was unreachable because jolt's deref dropped opts for records implementing IBlockingDeref, so no engine call was ever bounded. Fixed in proc/run and upstream as jolt-lang/jolt#537. Subprocesses are killable, unconditionally, which is the property the RAX-manager principle this loop follows actually asks for: the stop path must not depend on the component's cooperation. A hung PL_next_solution is not killable; a hung swipl is.\n\nReopen only if a specific engine shows a measured cost that a process boundary is demonstrably causing. Note that the last two things attributed to subprocess overhead both turned out to be something else: the Mathlib import cost was orphaned z3 processes, and the store contention was 0.027% of a turn.\n\nveriframe-clj-15u stays open and is detached from this. It is now a subprocess engine like the other three and no longer depends on any of this."}}
{"id":"int-5decdbf0","kind":"field_change","created_at":"2026-08-05T20:10:39.779477Z","actor":"Yogthos","issue_id":"veriframe-clj-u07","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Won't do. Subprocesses work reliably and the isolation they provide is worth more than anything FFI was going to buy.\n\nThe evidence accumulated against this from several directions rather than one:\n\nIsolation is a real feature, not just a fallback. Each branch gets its own process per engine, so a runaway or crashing engine costs one branch instead of the run. The input is model-generated and adversarial -- the linters exist because models emit encodings that are wrong in surprising ways -- which is exactly the case where a process boundary earns its keep. In-process, all branches share one heap and a segfault takes everything.\n\nThe FFI components produced this project's worst bugs, and all three were the same shape: a failure that is silent or a fault rather than an error. jolt-lang/db gave an invalid memory reference that failed all fourteen benchmark problems, from concurrent sqlite3_prepare_v2 on a shared handle. jolt-lang/http-client dropped the read timeout on the https path, so every provider call was unbounded. And the OpenSSL symbol-resolution hazard produced not a link error but a silent mix of LibreSSL and OpenSSL whose struct layouts disagreed, faulting on first use. A subprocess that misbehaves returns a bad exit code or hangs, and both are catchable.\n\nThe licence forecloses part of it anyway. veriframe is EPL-2.0 because jolt is EPL-2.0 without the secondary-licence option and three more dependencies are EPL-1.0 by inheritance from Clojure code. Linking a GPL engine in-process would force GPLv3, which is incompatible with that. Octave is out on those grounds regardless of engineering merit.\n\nThe per-engine case was weak even before that. SWI-Prolog's session.pl already returns structured JSON via library(http/json), so there is no text parsing for FFI to eliminate -- the current design already achieves what the epic was for. Lean's repl is a Lean program, so converting means reimplementing its frontend against a C API aimed at embedding the runtime. Z3 was the only clear win and it was gated on a P0 that has since been fixed by other means.\n\nAnd the thing that made subprocess bounding look unreliable was not the subprocess. proc/run's timeout branch was unreachable because jolt's deref dropped opts for records implementing IBlockingDeref, so no engine call was ever bounded. Fixed in proc/run and upstream as jolt-lang/jolt#537. Subprocesses are killable, unconditionally, which is the property the RAX-manager principle this loop follows actually asks for: the stop path must not depend on the component's cooperation. A hung PL_next_solution is not killable; a hung swipl is.\n\nReopen only if a specific engine shows a measured cost that a process boundary is demonstrably causing. Note that the last two things attributed to subprocess overhead both turned out to be something else: the Mathlib import cost was orphaned z3 processes, and the store contention was 0.027% of a turn.\n\nveriframe-clj-15u stays open and is detached from this. It is now a subprocess engine like the other three and no longer depends on any of this."}}
{"id":"int-287593ba","kind":"field_change","created_at":"2026-08-05T20:10:40.026424Z","actor":"Yogthos","issue_id":"veriframe-clj-5dn","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Won't do. Subprocesses work reliably and the isolation they provide is worth more than anything FFI was going to buy.\n\nThe evidence accumulated against this from several directions rather than one:\n\nIsolation is a real feature, not just a fallback. Each branch gets its own process per engine, so a runaway or crashing engine costs one branch instead of the run. The input is model-generated and adversarial -- the linters exist because models emit encodings that are wrong in surprising ways -- which is exactly the case where a process boundary earns its keep. In-process, all branches share one heap and a segfault takes everything.\n\nThe FFI components produced this project's worst bugs, and all three were the same shape: a failure that is silent or a fault rather than an error. jolt-lang/db gave an invalid memory reference that failed all fourteen benchmark problems, from concurrent sqlite3_prepare_v2 on a shared handle. jolt-lang/http-client dropped the read timeout on the https path, so every provider call was unbounded. And the OpenSSL symbol-resolution hazard produced not a link error but a silent mix of LibreSSL and OpenSSL whose struct layouts disagreed, faulting on first use. A subprocess that misbehaves returns a bad exit code or hangs, and both are catchable.\n\nThe licence forecloses part of it anyway. veriframe is EPL-2.0 because jolt is EPL-2.0 without the secondary-licence option and three more dependencies are EPL-1.0 by inheritance from Clojure code. Linking a GPL engine in-process would force GPLv3, which is incompatible with that. Octave is out on those grounds regardless of engineering merit.\n\nThe per-engine case was weak even before that. SWI-Prolog's session.pl already returns structured JSON via library(http/json), so there is no text parsing for FFI to eliminate -- the current design already achieves what the epic was for. Lean's repl is a Lean program, so converting means reimplementing its frontend against a C API aimed at embedding the runtime. Z3 was the only clear win and it was gated on a P0 that has since been fixed by other means.\n\nAnd the thing that made subprocess bounding look unreliable was not the subprocess. proc/run's timeout branch was unreachable because jolt's deref dropped opts for records implementing IBlockingDeref, so no engine call was ever bounded. Fixed in proc/run and upstream as jolt-lang/jolt#537. Subprocesses are killable, unconditionally, which is the property the RAX-manager principle this loop follows actually asks for: the stop path must not depend on the component's cooperation. A hung PL_next_solution is not killable; a hung swipl is.\n\nReopen only if a specific engine shows a measured cost that a process boundary is demonstrably causing. Note that the last two things attributed to subprocess overhead both turned out to be something else: the Mathlib import cost was orphaned z3 processes, and the store contention was 0.027% of a turn.\n\nveriframe-clj-15u stays open and is detached from this. It is now a subprocess engine like the other three and no longer depends on any of this."}}
{"id":"int-9d69e10d","kind":"field_change","created_at":"2026-08-05T20:18:50.938515Z","actor":"Yogthos","issue_id":"veriframe-clj-41m","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed in jolt-lang/http-client 0d56862, pinned here.\n\nThe premise held and was reproducible. Against a TLS server trickling one byte per second, a 3000ms :socket-timeout ran past two minutes and was still going, because every byte resets SO_RCVTIMEO. Worse than slow: nothing unwinds, so each attempt leaks a socket and a thread parked in the read loop.\n\nThe fix is a total deadline checked inside recv-all rather than the watchdog this bead proposed. That turned out to be the easier and better shape, and the reasoning is worth keeping: a trickling peer is precisely the one whose reads DO return, so the read loop gets control on every chunk and can check the clock itself. No socket handle needs exposing, which is what made the watchdog awkward. Total silence is the only case where the loop never gets control, and SO_RCVTIMEO already covers that, so the two bound the call from both ends. Throwing from inside the loop lets perform!'s existing finally close the stream, which is what stops the leak.\n\nConfigured via set-max-response-ms! rather than a request option, because clj-http-lite forwards only a fixed set of options to the connection. Default nil upstream so no other consumer changes; veriframe sets it in system/start! from HARNESS_MAX_RESPONSE_MS, default 600000.\n\nThat default is deliberately BELOW the 900000 turn deadline. The ordering is the substance of the fix: the innermost bound must fire first so the thread unwinds and releases its socket. If the scheduler's deadline wins the race it only abandons the branch's turn and the thread stays parked, which is the original failure with extra steps.\n\nVerified: bounded at 5049ms against a 5000ms cap with a typed SocketTimeoutException, while a normal https request is unaffected at 103ms. Regression test in jolt.http.timeout-test under the :timeouttest alias, with a one-byte-at-a-time TLS server.\n\nStill open and unchanged: :conn-timeout is ignored, because O_NONBLOCK needs variadic fcntl and Apple arm64 passes variadic args on the stack. connect is kernel-bounded by the SYN retry limit, unlike recv, so it is finite either way."}}
{"id":"int-e44b60f6","kind":"field_change","created_at":"2026-08-05T20:27:40.353736Z","actor":"Yogthos","issue_id":"veriframe-clj-oi4","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Won't do. Both premises this rested on were the z3 process leak, and neither survives measurement.\n\n## What this bead claimed\n\nThat `import Mathlib` costs 377927ms, that a second import is never cheaper, and that a beam of five therefore pays five separate six-minute imports competing for one machine.\n\n## What it actually costs, measured on a clean machine\n\n    1 session,  cold page cache    30807ms\n    3 sessions, concurrent         23013ms wall, 23011ms each\n\nTwo things follow, and both cut against this work.\n\nThe import is ~23-31 seconds, not six minutes. The earlier figures were taken while 28 orphaned z3 processes (veriframe-clj-qm7) held up to 450MB each on a 16GB machine already in swap, so Lean's ~7GB of mmapped oleans re-faulted on every import.\n\nConcurrent imports do not contend meaningfully. Three in parallel finished in the same wall-clock as one alone, and each individual import was FASTER than the single cold one because the pages were already resident. The work is mmap reads plus elaboration across cores, so it parallelises. \"Five branches competing for the same machine\" describes something that does not happen.\n\n## What sharing would actually buy, and cost\n\nBuy: approximately zero wall-clock, since the imports overlap, and 0.83GB per branch beyond the first.\n\nCost: send-command serialises on a compare-and-set busy flag and KILLS a session asked for something while busy. That is correct for one session per branch and fatal for a shared one, because any two branches calling Lean in the same round would kill it for everyone. Sharing needs a request queue with per-branch fairness, and the abandoned-turn problem that motivated kill-on-busy needs solving another way. It also weakens isolation: child environments do not give the guarantee a separate process does, so a branch leaving a broken instance or a `sorry` in scope could reach another, and that boundary would need its own test.\n\nA queue and an isolation model, to save memory on a path most problems never touch, is not a good trade.\n\n## Done instead\n\nThe cheap part, which was the real waste: a branch's engine sessions are now released when the branch goes inactive rather than at run end. A branch culled at turn 10 of 80 was holding 0.83GB for 70 turns it could not use. Committed separately.\n\nIf memory does become the constraint, the next cheapest moves are capping concurrent Lean sessions or lowering HARNESS_LEAN_WARM_SESSIONS, both of which are configuration rather than an architecture change.\n\n## Reopen if\n\nA beam is measured holding enough concurrent Lean sessions to matter on the target machine, AND capping the count is not acceptable. Note that Lean sessions are created lazily, so the worst case needs every branch to actually use Lean."}}
{"id":"int-4b80e86c","kind":"field_change","created_at":"2026-08-05T21:31:02.639781Z","actor":"Yogthos","issue_id":"veriframe-clj-15u","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Implemented in PR #5 (feat/octave-engine).\n\nShipped as a subprocess engine, one octave invocation per call. Verified end to end on a positive-definiteness and condition-number problem: 15 turns, none wasted, correct answer, independently checked (eigenvalues 2.194/3.387/9.419, cond 4.2922).\n\nTwo design points worth keeping.\n\nThe engine's epistemics are enforced in data, not prose. Octave computes rather than decides, so verify_octave takes the tolerance a claim is made at, records whether the arithmetic was exact, and writes the tolerance into the artifact claim. Shipped artifacts read '(numerically, within 0.005)' rather than presenting themselves as decisions. A check must reduce to a scalar logical: a matrix is refused rather than guessed at, empty is not false, NaN is not a verdict, and vf_approx is supplied because 0.1+0.1+0.1 == 0.3 is false.\n\nIt is NOT a persistent session, which is what this was designed as and what the other two subprocess engines use. Octave's fgetl(stdin) block-buffers on a pipe, so a request-per-line protocol deadlocks on the first request; confirmed by sending 8KB of pings to a session that had answered nothing, at which point every reply arrived at once. Per-invocation costs ~0.8s against a 37s turn and is the better shape anyway: no session to wedge, so none of the busy-flag machinery the Prolog engine needs, and each call is bounded and killable through engine/proc.clj.\n\nFound along the way and fixed in the same PR: the harness was discarding well-formed tool calls that lacked a fence. The first live run wasted 23 of 34 turns that way. parse-tool-call now accepts a trailing JSON object when no fence is present, narrowly, and records :unfenced?. Same problem after: 0 of 15.\n\nCI installs octave so the tests and probe actually run rather than skipping, since a skipped test reads identically to a passing one and that is how the Lean engine stayed green while every call to it failed."}}
{"id":"int-7a76d7fd","kind":"field_change","created_at":"2026-08-06T01:08:09.959583Z","actor":"Yogthos","issue_id":"vf-1bx.1","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"GAP lines, anti-contradiction clause, disagreement signal journaled; 82 tests green"}}
{"id":"int-0dd6c78d","kind":"field_change","created_at":"2026-08-06T01:16:49.554206Z","actor":"Yogthos","issue_id":"vf-1bx.2","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"consensus namespace + judge prompt + 7 contract tests; 89 tests green"}}
{"id":"int-9a52828a","kind":"field_change","created_at":"2026-08-06T01:34:38.889055Z","actor":"Yogthos","issue_id":"vf-1bx.3","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"MINOR/FIX parsing, fold-to-blocking rule, third disagreement direction, PATCHES in refusals; 91 tests green"}}
{"id":"int-8f13c770","kind":"field_change","created_at":"2026-08-06T01:42:17.302806Z","actor":"Yogthos","issue_id":"vf-1bx.4","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"exemption list wired to both judges + digest; 93 tests green"}}
{"id":"int-ca97cb55","kind":"field_change","created_at":"2026-08-06T01:51:12.242298Z","actor":"Yogthos","issue_id":"vf-1bx.5","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"ESTABLISHED/RELAXATION parsing, thesis-drift journal, done-gate relaxation refusal; 98 tests green"}}
{"id":"int-f2cea6cf","kind":"field_change","created_at":"2026-08-06T02:01:17.340915Z","actor":"Yogthos","issue_id":"vf-1bx.6","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"wind-down gate, prompt, predictions, tests; 99 tests green"}}
{"id":"int-dd4ec229","kind":"field_change","created_at":"2026-08-06T02:10:19.318362Z","actor":"Yogthos","issue_id":"vf-1bx.7","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"report assembly + rendering + API wiring + honesty tests; 100 tests green"}}
{"id":"int-734b38fb","kind":"field_change","created_at":"2026-08-06T02:21:55.314611Z","actor":"Yogthos","issue_id":"vf-1bx.8","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"store twin of failure log + flag + loop wiring + journal hits + bench threading; 102 tests green"}}
{"id":"int-32e76448","kind":"field_change","created_at":"2026-08-06T02:33:18.829793Z","actor":"Yogthos","issue_id":"vf-1bx.9","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"claim registry + review/verify_template wiring + journal + tests; 104 tests green"}}
{"id":"int-c816856a","kind":"field_change","created_at":"2026-08-06T02:43:29.218097Z","actor":"Yogthos","issue_id":"vf-1bx.10","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"rank-finished + select-done-branch + journal + tests; 110 tests green"}}
{"id":"int-bfa6e692","kind":"field_change","created_at":"2026-08-06T03:01:40.12524Z","actor":"Yogthos","issue_id":"vf-1bx.11","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"resume ns + run-rounds split + endpoint + tests; 112 tests green"}}
{"id":"int-b1011c61","kind":"field_change","created_at":"2026-08-06T03:01:41.150126Z","actor":"Yogthos","issue_id":"vf-1bx","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"all 11 issues landed; suite 72->112 tests across the epic"}}
{"id":"int-82631513","kind":"field_change","created_at":"2026-08-06T13:32:47.668506Z","actor":"Yogthos","issue_id":"vf-emt","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"context-block dedupes hit journaling per (artifact id, branch) via :shared-served branch memory; resume limit documented"}}
{"id":"int-542bd6de","kind":"field_change","created_at":"2026-08-06T17:45:39.532844Z","actor":"Yogthos","issue_id":"vf-nd2","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"system.md now says four engines"}}
{"id":"int-556464ad","kind":"field_change","created_at":"2026-08-06T22:33:04.355664Z","actor":"Yogthos","issue_id":"vf-42e","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"judge retries unparseable verdicts in-call with sharper instruction and doubled tokens; transport failures still fail closed"}}
{"id":"int-ce93c15a","kind":"field_change","created_at":"2026-08-06T23:16:53.039529Z","actor":"Yogthos","issue_id":"vf-691","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"done with no answer ships the last passing audit's approved text; explicit answers keep the verbatim rule"}}
{"id":"int-4738cede","kind":"field_change","created_at":"2026-08-07T01:12:13.816102Z","actor":"Yogthos","issue_id":"vf-huj","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Closed"}}
{"id":"int-001940af","kind":"field_change","created_at":"2026-08-07T01:12:14.053779Z","actor":"Yogthos","issue_id":"vf-b1f","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Closed"}}
{"id":"int-17d9b66f","kind":"field_change","created_at":"2026-08-07T01:44:46.075668Z","actor":"Yogthos","issue_id":"vf-doq","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"landed with tests; 129 tests green"}}
{"id":"int-2f6d58c3","kind":"field_change","created_at":"2026-08-07T03:34:25.587964Z","actor":"Yogthos","issue_id":"vf-9vj","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"gui/ root behind :gui alias, glimmer+glimmer-gl pinned, window shell launches clean; headless suite unaffected (129/666 green)"}}
{"id":"int-830d2519","kind":"field_change","created_at":"2026-08-07T03:58:53.12982Z","actor":"Yogthos","issue_id":"vf-b46","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"toolkit-free api client + pure poll fold, tested headless (131/686); shell tails the newest run live"}}
{"id":"int-ea63d085","kind":"field_change","created_at":"2026-08-07T04:07:49.065799Z","actor":"Yogthos","issue_id":"vf-cht","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"pure fold + edges with seed provenance, defensive on order and unknown kinds; 133/702 green"}}
{"id":"int-76c708a6","kind":"field_change","created_at":"2026-08-07T04:32:01.580909Z","actor":"Yogthos","issue_id":"vf-yls","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"GL pane draws the folded graph with exact picking; branch log, intervention bar, and run controls wired to the API"}}
{"id":"int-e57d5b0d","kind":"field_change","created_at":"2026-08-07T04:32:01.810471Z","actor":"Yogthos","issue_id":"vf-bku","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"GL pane draws the folded graph with exact picking; branch log, intervention bar, and run controls wired to the API"}}
{"id":"int-1d79f803","kind":"field_change","created_at":"2026-08-07T04:32:02.049085Z","actor":"Yogthos","issue_id":"vf-bsc","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"GL pane draws the folded graph with exact picking; branch log, intervention bar, and run controls wired to the API"}}
{"id":"int-b5bd50de","kind":"field_change","created_at":"2026-08-07T04:32:02.278592Z","actor":"Yogthos","issue_id":"vf-se8","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"GL pane draws the folded graph with exact picking; branch log, intervention bar, and run controls wired to the API"}}
{"id":"int-815dcb6a","kind":"field_change","created_at":"2026-08-07T04:32:18.252242Z","actor":"Yogthos","issue_id":"vf-e83","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"all seven child tasks complete: scaffold, api+poller, graph fold, GL pane, picking+log, interventions, run controls"}}
{"id":"int-2eeb5bc5","kind":"field_change","created_at":"2026-08-07T14:15:02.707657Z","actor":"Yogthos","issue_id":"vf-m40","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"gens 6-9 complete. gen9 produced the campaign's strongest result: a {3,5,7}-supported density-feasible set proved non-covering by a Lean-checked partition argument on a search space of 1.7e9 assignments. Evolutionary loop shipped and validated live (forked: 2)."}}
{"id":"int-61773103","kind":"field_change","created_at":"2026-08-07T23:06:49.584188Z","actor":"Yogthos","issue_id":"vf-mti","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Closed"}}
{"id":"int-c7d72462","kind":"field_change","created_at":"2026-08-07T23:06:49.848332Z","actor":"Yogthos","issue_id":"vf-s4p","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Closed"}}
{"id":"int-46e564dc","kind":"field_change","created_at":"2026-08-08T00:32:24.324892Z","actor":"Yogthos","issue_id":"vf-817","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Closed"}}
{"id":"int-14aef3a1","kind":"field_change","created_at":"2026-08-08T03:49:02.481306Z","actor":"Yogthos","issue_id":"vf-92l","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed in e0d30c3: run-rounds journals run-error and marks the row failed before rethrowing; start-run! drops the dead handle; runs/stalled? plus last_progress_at exposed on the run endpoint. Verified against the dead gen-11 run, which now reports stalled=true instead of claiming to be running."}}
{"id":"int-9e68c5e8","kind":"field_change","created_at":"2026-08-08T07:13:04.128258Z","actor":"Yogthos","issue_id":"vf-0nl","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed in eda36a4: beam/child-ids allocates from the branches table, skipping taken suffixes. Tests cover the allocator and a live double fork. This was also the unidentified gen-11 crash from vf-92l."}}
{"id":"int-330f28c7","kind":"field_change","created_at":"2026-08-08T11:54:32.654322Z","actor":"Yogthos","issue_id":"vf-pmj","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed in 965b459: prolog artifacts carry the assert log with the goal, would-be confirmations go through the same faithfulness judge as verify_smt (with a note naming findall/3, forall/2 and \\+ as contexts that undo constraint posts), and the ground-goal path no longer reaches :confirmed on the strength of merely succeeding. Tests cover all three. NOT YET DEPLOYED to the running server — needs a restart to take effect."}}
{"id":"int-af97447f","kind":"field_change","created_at":"2026-08-08T14:36:45.565182Z","actor":"Yogthos","issue_id":"vf-hj2","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed in a701e51: call-model retries once at a doubled budget when the response hit the token cap before emitting a tool call. Bounded to one retry; truncation that still carried a call is not retried. Journalled as :turn-retry."}}
{"id":"int-ced504f0","kind":"field_change","created_at":"2026-08-08T14:36:46.786311Z","actor":"Yogthos","issue_id":"vf-5wi","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed across 965b459 and a701e51. The 'or restate the claim to match what you encoded' escape is gone from the :unfaithful message. More importantly the drift incentive is largely removed: the deterministic checks in agent.faithful cannot be argued with, so rewriting the claim no longer buys a confirmation past a bad coefficient or a missing constraint. Known limitation, pinned by test: a claim stating FEWER constraints than the encoding asserts (gen-12's 14-vs-15) is not detectable, since a surplus of asserts is what correct artifacts look like."}}
{"id":"int-0bb49f4f","kind":"field_change","created_at":"2026-08-08T14:36:47.430034Z","actor":"Yogthos","issue_id":"vf-slr","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed across 965b459 and a701e51. Coverage: verify_smt and verify (prolog) now run deterministic structural checks BEFORE the LLM judge, and the judge after. Validated against the real artifacts — a#333, a#338, a#343 rejected; a#337, a#345 accepted; no false positives. Remaining gaps tracked in vf-2ky: verify_lean, verify_octave and verify_template are still judge-free, and refutations skip both layers."}}
{"id":"int-de0d01e4","kind":"field_change","created_at":"2026-08-08T15:20:09.792115Z","actor":"Yogthos","issue_id":"vf-djx","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Octave, Lean and verify_template now run structural checks then the judge, and refutations face both layers. New deterministic checks: octave expressions that compute nothing (a#344), unsat over an undisclosed partial family pin (a#336), and claims whose enumerated index families exceed their stated total (a#321/a#324, the case the assert count could not reach). Validated against all 348 artifacts in the journal: 1 of 42 octave rows objected to, 1 of 108 smt rows, 2 of 348 claims — every one a known-bad artifact, zero false positives. Also fixed: a Lean snippet the REPL rejects no longer records a :refuted artifact, and verify_template no longer claims its cross-check makes review unnecessary."}}
{"id":"int-44cb6d57","kind":"field_change","created_at":"2026-08-08T16:57:19.663293Z","actor":"Yogthos","issue_id":"vf-q62","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed in 8de8632: note-verdict! inside judge/2 records label, verdict, named gaps, prompt size and the answer (reasoning stripped, capped at 4000 chars) for every judge call — faithfulness, review and audit alike, passes as well as failures, with transport failures distinguishable from rejections on the merits. Tests: a-judge-verdict-is-journalled-with-its-reasoning, a-judge-verdict-that-passes-is-journalled-too.\n\nThe motivating question is now answered, offline, by reconstructing the prompt: the judge was RIGHT both times. Its gap on a#350 — 'the claim is for support {3,5,7,11}, but the artifact never uses the prime 11' — is correct, since 11 is coprime to 105 and enters the argument only through the eight scaled constants, which neither artifact derives. Not deployed: needs a jolt serve restart, which would end gen-14."}}
{"id":"int-de4a6fad","kind":"field_change","created_at":"2026-08-08T20:58:39.620071Z","actor":"Yogthos","issue_id":"vf-70o","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"explain-check-error in engine/octave.clj maps the two signatures gen-14 kept hitting — 'invalid use of statement list' and \"'NAME' undefined\" — onto messages that name octave_eval as where statements and helper definitions belong, and keep Octave's own text appended. Everything else passes through. Wired into check, so it applies wherever verify_octave is called. Tests: an-octave-error-says-what-expr-is-actually-for (pure, runs without octave) and octave-check-explains-a-statement-list-error (end to end)."}}
{"id":"int-ad9df261","kind":"field_change","created_at":"2026-08-09T00:33:26.436184Z","actor":"Yogthos","issue_id":"vf-7hg","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"journal/branch-turns filters by branch in SQL (using the idx_turns_run index, which was going unused) and drops assistant_text/reasoning_text from the projection — 5,857,814 bytes to 69,366, an 84x reduction. journal/turns keeps its full-row shape because resume replays assistant_text from it. Client side: refresh-branch-log! now records the failure in :branch-log-error and the panel says why it is missing, noting the live activity and attempts above it are complete; and the poll loop compares against selected-branch-id so artifact panels refresh too. Tests: branch-turns-reads-one-branch-and-leaves-the-bulk-behind."}}
{"id":"int-f57c338f","kind":"field_change","created_at":"2026-08-09T05:23:59.992876Z","actor":"Yogthos","issue_id":"vf-c49","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed and hot-reloaded into the live run over nREPL without restarting it. Test: an-unfaithful-prolog-artifact-is-reported-as-a-failure-with-the-reason."}}
{"id":"int-70bbbcfa","kind":"field_change","created_at":"2026-08-09T05:38:24.027254Z","actor":"Yogthos","issue_id":"vf-i26","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed and hot-reloaded into the live run over nREPL. Test: a-rejection-quotes-the-objection-to-the-branch-and-the-failure-log."}}
{"id":"int-cbd8c3ad","kind":"field_change","created_at":"2026-08-09T11:43:39.088624Z","actor":"Yogthos","issue_id":"vf-0of","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Closed"}}
{"id":"int-96f96078","kind":"field_change","created_at":"2026-08-09T11:43:39.360105Z","actor":"Yogthos","issue_id":"vf-eq9","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Closed"}}
{"id":"int-7ed2f015","kind":"field_change","created_at":"2026-08-09T12:36:23.828902Z","actor":"Yogthos","issue_id":"vf-8fl","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Closed"}}
{"id":"int-a1096ae8","kind":"field_change","created_at":"2026-08-09T13:42:29.452356Z","actor":"Yogthos","issue_id":"vf-9p2","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Closed"}}
{"id":"int-479632fb","kind":"field_change","created_at":"2026-08-09T15:22:35.421896Z","actor":"Yogthos","issue_id":"vf-w2k","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Closed"}}
{"id":"int-8379ef5d","kind":"field_change","created_at":"2026-08-09T16:45:52.393008Z","actor":"Yogthos","issue_id":"vf-kpn","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Closed"}}
{"id":"int-81694a7b","kind":"field_change","created_at":"2026-08-10T00:23:33.044623Z","actor":"Yogthos","issue_id":"vf-b9c","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Done gate now reads run-wide confirmed+empirical artifacts, not just the branch's own. Deployed into run 0d0c3560 over nREPL."}}
{"id":"int-5afe6578","kind":"field_change","created_at":"2026-08-10T01:52:41.319341Z","actor":"Yogthos","issue_id":"vf-itz","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed, tested, deployed into run 65d50333 over nREPL."}}
{"id":"int-5d5b4185","kind":"field_change","created_at":"2026-08-10T01:52:41.560264Z","actor":"Yogthos","issue_id":"vf-1wz","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed, tested, deployed into run 65d50333 over nREPL."}}
{"id":"int-f1f8c0df","kind":"field_change","created_at":"2026-08-10T03:08:03.034072Z","actor":"Yogthos","issue_id":"vf-5xp","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed, tested, deployed into run 65d50333 over nREPL."}}
{"id":"int-018d62a4","kind":"field_change","created_at":"2026-08-10T06:33:24.584191Z","actor":"Yogthos","issue_id":"vf-1u5","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"strip-lean-imports added, tested, deployed into run 65d50333 over nREPL."}}
{"id":"int-0c179a66","kind":"field_change","created_at":"2026-08-12T14:44:52.100296Z","actor":"Yogthos","issue_id":"vf-gff","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Root-caused to jolt's quadratic StringBuilder/StringWriter append and fixed there (jolt fix/stringbuilder-quadratic-append, c22ce774). Verified: sbperf 16.1x -> 3.2x scaling, jolt unit clean, corpus 4323/4332 with 0 new divergence, cts 6078 matching baseline, veriframe suite 240 tests/1171 assertions clean under the fixed binary. Run-detail endpoint 322.7s -> 3.2s. No veriframe code change was needed for either."}}
{"id":"int-4c913abf","kind":"field_change","created_at":"2026-08-12T14:44:52.363968Z","actor":"Yogthos","issue_id":"vf-8g2","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Root-caused to jolt's quadratic StringBuilder/StringWriter append and fixed there (jolt fix/stringbuilder-quadratic-append, c22ce774). Verified: sbperf 16.1x -> 3.2x scaling, jolt unit clean, corpus 4323/4332 with 0 new divergence, cts 6078 matching baseline, veriframe suite 240 tests/1171 assertions clean under the fixed binary. Run-detail endpoint 322.7s -> 3.2s. No veriframe code change was needed for either."}}
{"id":"int-ba654328","kind":"field_change","created_at":"2026-08-12T23:27:37.1775Z","actor":"Yogthos","issue_id":"vf-04l","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"jolt 0.7.6 is installed (Cellar/jolt/0.7.6, symlinked at /opt/homebrew/bin/jolt) and carries a1856fbe — the StringBuilder fix rode into main on PR #593. Verified against the installed binary: run-detail 322740ms -> 3344ms in-process and 3.33s through HTTP, 88KB witness read 352ms / write 255ms, veriframe suite 240 tests / 1171 assertions clean."}}
{"id":"int-3668b59d","kind":"field_change","created_at":"2026-08-12T23:48:28.006803Z","actor":"Yogthos","issue_id":"vf-k0a","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed in vendor/ring_chez/adapter.clj: close-on-exec! marks the listen fd and every accepted connection with FD_CLOEXEC via a :varargs fcntl binding (SOCK_CLOEXEC is Linux-only). Covered by server_test/a-subprocess-does-not-inherit-the-listen-socket, which spawns a child, stops the server and rebinds — red before (bind() failed on port 39187), green after. Verified live: lsof on a running server now shows only jolt on 3985, with the Lean repl warm and Mathlib imported."}}
{"id":"int-e609f66b","kind":"field_change","created_at":"2026-08-12T23:48:54.99009Z","actor":"Yogthos","issue_id":"vf-36o","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed and verified end to end: start-timeout-ms 40000 outlasts the server's 30s window, gui_api_test asserts it stays above 30000, and a 21095ms start now succeeds where it previously returned 'Read timed out'."}}
{"id":"int-f2ca9460","kind":"field_change","created_at":"2026-08-13T00:06:59.347527Z","actor":"Yogthos","issue_id":"vf-d0a","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed by shutdown() before close() in vendor/ring_chez/adapter.clj; CI green on all four jobs."}}
{"id":"int-e4eb64fc","kind":"field_change","created_at":"2026-08-13T00:22:01.535699Z","actor":"Yogthos","issue_id":"vf-60k","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Superseded by vf-g2l, which covers this row and gen-18's together. Same defect, filed twice."}}
{"id":"int-a6e620bd","kind":"field_change","created_at":"2026-08-13T00:41:48.469851Z","actor":"Yogthos","issue_id":"vf-haw","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"docs/phase-unwrapping-2.md covers both runs: the three-stage rule and why stage 2 is load-bearing, gen-18's Q-A/Q-B/Q-C/Q-D results with their restrictions, the vf-izq gap (poly-time algorithm excludes the tied-path mechanism), and vf-6ai (12-vertex graph described as 16-vertex, passed by both gates). 3x3 funnel independently re-derived over the cycle space."}}
{"id":"int-50934d21","kind":"field_change","created_at":"2026-08-13T00:56:37.715616Z","actor":"Yogthos","issue_id":"vf-4ja","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"on-start now fires before the beam opens its branches, so POST /v1/runs returns as soon as the run row exists rather than after a Prolog session is spawned per branch. Covered by agent_test/on-start-fires-before-the-beam-opens-its-branches."}}
{"id":"int-8ef730a8","kind":"field_change","created_at":"2026-08-13T00:56:38.498459Z","actor":"Yogthos","issue_id":"vf-cb0","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Repopulation is now the :repopulate gate (priority 6, above branch-out): the scheduler marks the strongest survivor and the gate does the asking, so it carries a prediction, settles met/unmet on branch_theses, appears in the gate tally, and cannot land on a boundary that already had a steer. Budget :max-repopulates. Covered by agent_test/repopulation-is-a-gate-with-a-prediction-not-an-invitation."}}
{"id":"int-99dad738","kind":"field_change","created_at":"2026-08-13T00:58:08.949244Z","actor":"Yogthos","issue_id":"vf-g2l","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed at the mechanism rather than by correcting rows: system/start! calls runs/reconcile-orphans!, which marks every row still claiming to run as interrupted with an ended_at. Sound only at startup, which is when it runs, because the beam only ever runs in this process. Verified live — the server logged 'marked 2 run(s) interrupted' and both gen-18 (65d50333) and gen-11 (90336412) now read interrupted. Covered by store_test/a-run-left-running-by-a-crash-is-reconciled-at-startup."}}
{"id":"int-d6ae8455","kind":"field_change","created_at":"2026-08-13T01:12:44.404575Z","actor":"Yogthos","issue_id":"vf-6ai","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Acceptance criteria met: docs/phase-unwrapping-2.md states the graph has twelve vertices, notes that gen-17's B4 shipped '16-vertex', and records why both gates passed it — claim and code agreed because the code's own Nv=16 left rows 13..16 all-zero, so the constraint on them read 0==0, and the coverage rung found 16 in the (mislabelled) evidence. The bead proposed no code fix by design: a mechanical 'does the prose vertex count match the edge list' check is not worth building for a decorative constant. Numbers unaffected — 8 flows at cost 6 both verified."}}
{"id":"int-2427cf45","kind":"field_change","created_at":"2026-08-13T01:32:27.74147Z","actor":"Yogthos","issue_id":"vf-38b","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed at the mechanism rather than by enumerating the ways a widget can die. glpane now connects to the gl-area's own GTK 'unrealize' signal via glimmer.ffi/g-signal-connect-data and clears the cached pointer and GL objects when it fires; realize! puts them back. That covers every teardown — the new-run form (which renders a tree with no :gl-area), glimmer's positional reconciler destroying a child when a slot's tag changes, and reparenting — without needing a hook glimmer does not have. The callable is retained, since a collected one leaves GTK holding a dangling function pointer, and each widget is wired once.\n\nVerified: handler id came back 404 (nonzero — an invalid signal name returns 0 and warns), no gsignal warning, and a 60s GUI session against a live run produced an entirely empty log where the same window previously produced a 13-critical burst. The replacement-detection println is kept as a backstop, since the only evidence this bug ever gave was a GTK critical with nothing naming the transition."}}
{"id":"int-cf569ca1","kind":"field_change","created_at":"2026-08-13T01:32:29.15527Z","actor":"Yogthos","issue_id":"vf-966","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Both deviations brought onto the API's existing convention — a real status plus {:error {:message ...}}, which is what 'no such run', 'no such branch', the 404 fallback, the 500 handler and resume's 409 already did. abort on a non-active run is now 409 (matching resume's 'not resumable': the run exists, it is just not in an abortable state); a start that does not come up in 30s is 503 (the request was fine, the server could not service it).\n\nBoth now use resume's envelope — success {:body ...}, refusal {:status N :body ...} — so one route shape serves both. That envelope is load-bearing: the abort success body carries :status \"aborting\", the RUN's state, and a route reading (:status r) off a bare map would have sent the string \"aborting\" as the HTTP status. My first attempt did exactly that and the test caught it.\n\nVerified live over nREPL without restarting: POST /v1/runs/no-such-run/abort returns 409 with {\"error\":{\"message\":\"no active run ...\"},\"run_id\":\"...\"}."}}
{"id":"int-4febc741","kind":"field_change","created_at":"2026-08-13T13:04:37.979976Z","actor":"Yogthos","issue_id":"vf-7l6","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed in jolt 0.7.9 (7bf6c497 + CI test b9c4753e). Verified on the live server: lake/repl forked with both listeners open and inherited neither; only jolt holds 7888 fd 9 and 3985 fd 4."}}
{"id":"int-e9727aa7","kind":"field_change","created_at":"2026-08-13T14:28:48.429442Z","actor":"Yogthos","issue_id":"vf-jki","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed and deployed over nREPL into the running gen-20 without interrupting it. 248 tests, 1215 assertions, 0 failures. Verified live: record-outcome on B2's exact sequence gives consecutive-failures 0, mechanics 3 (under the 6 threshold), and is nil-safe for the four branches already in flight. B2 itself stays culled — a culled branch cannot be reopened and the beam no longer holds it — so gen-20 runs at 4 branches instead of 5."}}
{"id":"int-366eeca2","kind":"field_change","created_at":"2026-08-13T16:26:21.242572Z","actor":"Yogthos","issue_id":"vf-4ef","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Implemented and tested. Usage capture turned out bigger than filed: the adapter parsed usage and client/chat returned it, but the agent loop dropped it and turns had no columns, so the harness could not report what a run cost at all. Migration v4 adds prompt/completion/total plus the cache split, nullable so a provider-error turn does not claim to be free."}}
{"id":"int-99492b52","kind":"field_change","created_at":"2026-08-13T16:26:21.504975Z","actor":"Yogthos","issue_id":"vf-0se","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Implemented and tested. Usage capture turned out bigger than filed: the adapter parsed usage and client/chat returned it, but the agent loop dropped it and turns had no columns, so the harness could not report what a run cost at all. Migration v4 adds prompt/completion/total plus the cache split, nullable so a provider-error turn does not claim to be free."}}
{"id":"int-0d13c7d9","kind":"field_change","created_at":"2026-08-13T16:26:21.751851Z","actor":"Yogthos","issue_id":"vf-mm0","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Implemented and tested. Usage capture turned out bigger than filed: the adapter parsed usage and client/chat returned it, but the agent loop dropped it and turns had no columns, so the harness could not report what a run cost at all. Migration v4 adds prompt/completion/total plus the cache split, nullable so a provider-error turn does not claim to be free."}}
{"id":"int-34b2f089","kind":"field_change","created_at":"2026-08-13T16:36:09.879712Z","actor":"Yogthos","issue_id":"vf-614","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Implemented and tested. Adapter protocol gained prefill-support?; the OpenAI-family adapter appends the prefix as a trailing assistant message flagged :prefix true, and only for DeepSeek — plain OpenAI, local and Ollama declare false and produce byte-identical bodies to today. client/chat now names :prefill in its opts, which it did not, so without that the whole path would have been dead code; there is a test pinning it. fence/parse-tool-call takes an opts map and reattaches the prefix before matching, skipping reattachment when the model repeated the opener itself. Non-prefilled parsing is unchanged, asserted by equality against the no-opts arity. 252 tests, 1255 assertions, 0 failures."}}
{"id":"int-2e1b5223","kind":"field_change","created_at":"2026-08-13T16:36:10.146254Z","actor":"Yogthos","issue_id":"vf-24a","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Implemented and tested. Adapter protocol gained prefill-support?; the OpenAI-family adapter appends the prefix as a trailing assistant message flagged :prefix true, and only for DeepSeek — plain OpenAI, local and Ollama declare false and produce byte-identical bodies to today. client/chat now names :prefill in its opts, which it did not, so without that the whole path would have been dead code; there is a test pinning it. fence/parse-tool-call takes an opts map and reattaches the prefix before matching, skipping reattachment when the model repeated the opener itself. Non-prefilled parsing is unchanged, asserted by equality against the no-opts arity. 252 tests, 1255 assertions, 0 failures."}}
{"id":"int-a7c42072","kind":"field_change","created_at":"2026-08-13T16:56:26.570485Z","actor":"Yogthos","issue_id":"vf-9ez","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Run completed and verified. Q-1 not settled; three lemmas confirmed independently. vf-izq stays open."}}
{"id":"int-7325a98b","kind":"field_change","created_at":"2026-08-13T18:27:44.663975Z","actor":"Yogthos","issue_id":"vf-e7g","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Two fixes, both TDD. (1) missing-argument errors now show the call they wanted — 'A call to `proof_start` looks like: {\"name\": \"proof_start\", \"args\": {\"claim\": \"<claim>\", \"theorem\": \"<theorem>\"}}' — built from the tool name and required keys the function is already handed, so no schema registry. (2) state/repeating-failure? compares the last two turns on exact (tool, error) and the loop appends an escalation telling the branch to change the call or the technique. Exact comparison, no threshold, cannot fire on a retry that changed anything or on a branch that succeeded in between. Resume does not replay the per-turn :error — noted in resume.clj's fidelity contract, because the turns table holds the ESCALATED text which would not compare equal and would break the detection it was meant to restore. 254 tests, 1267 assertions, 0 failures."}}
{"id":"int-a77f2f89","kind":"field_change","created_at":"2026-08-13T18:36:24.656136Z","actor":"Yogthos","issue_id":"vf-yxs","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Both fixed, TDD, 257 tests / 1276 assertions / 0 failures.\n\nvf-8wp: render now carries the code, truncated at 700 chars — enough for a Lean signature with a dozen hypotheses, small enough that five entries cannot crowd the branch's own context, since the block re-renders every turn. The code was already selected by similar/recent and copied by seed-from-run! for exactly this purpose; only the renderer dropped it. Block also now says 'Cite these by name and re-verify in one call; do not re-derive them.'\n\nvf-yxs: fixed in two places, because ranking alone was not enough. artifacts/prefer-in-run puts in-run entries before seeded ones preserving relevance order within each group, AND context-block now fetches 3x what it displays before ranking down — reordering a top-5 that is already all seeds just reorders seeds, which is the normal early case since a completed run contributes its whole pool at turn 0.\n\nCORRECTION to my filed design: I had proposed dropping seeded artifacts from the block entirely, on the grounds that seeding already injects them into the problem statement. That was wrong. Seeding does not touch the problem statement — gen-20's statement restated gen-19's results because I wrote it that way by hand. A campaign that seeds without restating would have lost them. Ranked, not filtered."}}
{"id":"int-57ef7e17","kind":"field_change","created_at":"2026-08-13T18:36:24.898866Z","actor":"Yogthos","issue_id":"vf-8wp","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Both fixed, TDD, 257 tests / 1276 assertions / 0 failures.\n\nvf-8wp: render now carries the code, truncated at 700 chars — enough for a Lean signature with a dozen hypotheses, small enough that five entries cannot crowd the branch's own context, since the block re-renders every turn. The code was already selected by similar/recent and copied by seed-from-run! for exactly this purpose; only the renderer dropped it. Block also now says 'Cite these by name and re-verify in one call; do not re-derive them.'\n\nvf-yxs: fixed in two places, because ranking alone was not enough. artifacts/prefer-in-run puts in-run entries before seeded ones preserving relevance order within each group, AND context-block now fetches 3x what it displays before ranking down — reordering a top-5 that is already all seeds just reorders seeds, which is the normal early case since a completed run contributes its whole pool at turn 0.\n\nCORRECTION to my filed design: I had proposed dropping seeded artifacts from the block entirely, on the grounds that seeding already injects them into the problem statement. That was wrong. Seeding does not touch the problem statement — gen-20's statement restated gen-19's results because I wrote it that way by hand. A campaign that seeds without restating would have lost them. Ranked, not filtered."}}
{"id":"int-44dc1977","kind":"field_change","created_at":"2026-08-13T19:50:38.593599Z","actor":"Yogthos","issue_id":"vf-00b","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Implemented, tested and verified end-to-end against the live API.\n\nGates that predict exactly one tool (branch-out, repopulate — both 'the branch calls branch_theses') carry :tool; arbiter/decide passes it through and arbiter/prefill-for turns it into '```tool-call\\n{\"name\": \"branch_theses\"'. Gates naming a choice (milestone: review-or-done) or a behaviour (stuck) get the bare fence, which still forecloses prose without the harness picking for the branch. Scoped to gated turns only — prefilling makes the response BEGIN with the call, which is right when already steering and wrong as a blanket default.\n\nCORRECTION to what I filed here. I wrote that /v1 would ignore the prefix silently. It does not: DeepSeek rejects the request outright with 'prefix is only available when using beta api'. Safer, but it means a wrong base URL would fail EVERY steered turn, so supports-prefill? now takes config and checks for /beta — a /v1 config simply does not prefill and behaves exactly as today. Default base-url moved to /beta after verifying it serves ordinary completions identically.\n\nTwo bugs caught while wiring it, both of which would have been silent: the assistant message and journal assistant_text stored only the completion, leaving turns beginning mid-fence in the transcript and misrepresenting the format back to the model (fence/reattach now rebuilds the whole turn in both places); and truncated-without-call? re-parsed without the prefill, so a prefilled turn that HAD issued a call would have been billed a retry.\n\nLive check: prefill '{\"name\": \"branch_theses\"' returned '}' and parsed as branch_theses with no parse error. 259 tests, 1290 assertions, 0 failures."}}
{"id":"int-9e135397","kind":"field_change","created_at":"2026-08-13T19:58:12.981368Z","actor":"Yogthos","issue_id":"vf-a8h","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Measured across 18 runs / 3,464 consecutive turn pairs (far more than the four originally scoped — every run with assistant_text populated). Recommendation: DO NOT build the prose detector. Details on vf-ap8."}}
{"id":"int-bbe0dca7","kind":"field_change","created_at":"2026-08-13T19:58:14.607574Z","actor":"Yogthos","issue_id":"vf-ap8","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Measured rather than built. Prose repetition does not occur in this harness: 0 of 3,464 consecutive turn pairs exceed 0.7 Jaccard, median 0.008, max 0.694. A detector would never fire. The real repetition is structured — identical (tool, error) on 29% of no-call turns — and is handled by vf-e7g. Numbers recorded in the notes."}}
{"id":"int-73141c24","kind":"field_change","created_at":"2026-08-13T20:58:01.441182Z","actor":"Yogthos","issue_id":"vf-0tk","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Built as one mechanism instead of three. journal/ledger reads the artifacts table — which already has claim_status where shared_artifacts does not — so refutations become visible with no migration and no second sharing path; that is vf-0tk solved via the ledger rather than by widening shareable?, which I had filed. Complete rather than FTS-sampled, since the value is that a branch can trust the absence of a line. Two sections so a refutation cannot read as a confirmation. fetch_artifact makes the ids actionable, :neutral so a lookup cannot clear a failure count, run-scoped, and works on refuted artifacts where the encoding is what shows why the line is closed.\n\nTHE PART THAT NEARLY WENT WRONG: the block is appended every turn, so a naive wiring accumulates one copy per turn — gen-17's ledger over an 80-turn branch would have been 854,800 chars / 211k tokens, four times the largest context ever recorded. message/strip-stale-ledgers drops all but the newest on the way to the wire, following the strip-think-blocks precedent; the branch keeps every copy so the journal and resume stay faithful. 80 turns now costs 10,642 chars instead of 854,800. Claims capped at 180 chars with the full statement one fetch away: gen-18's ledger 6,795 -> 3,805 tokens, gen-20's 428.\n\n264 tests, 1324 assertions, 0 failures."}}
{"id":"int-81919a97","kind":"field_change","created_at":"2026-08-13T20:58:01.765861Z","actor":"Yogthos","issue_id":"vf-9e3","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Built as one mechanism instead of three. journal/ledger reads the artifacts table — which already has claim_status where shared_artifacts does not — so refutations become visible with no migration and no second sharing path; that is vf-0tk solved via the ledger rather than by widening shareable?, which I had filed. Complete rather than FTS-sampled, since the value is that a branch can trust the absence of a line. Two sections so a refutation cannot read as a confirmation. fetch_artifact makes the ids actionable, :neutral so a lookup cannot clear a failure count, run-scoped, and works on refuted artifacts where the encoding is what shows why the line is closed.\n\nTHE PART THAT NEARLY WENT WRONG: the block is appended every turn, so a naive wiring accumulates one copy per turn — gen-17's ledger over an 80-turn branch would have been 854,800 chars / 211k tokens, four times the largest context ever recorded. message/strip-stale-ledgers drops all but the newest on the way to the wire, following the strip-think-blocks precedent; the branch keeps every copy so the journal and resume stay faithful. 80 turns now costs 10,642 chars instead of 854,800. Claims capped at 180 chars with the full statement one fetch away: gen-18's ledger 6,795 -> 3,805 tokens, gen-20's 428.\n\n264 tests, 1324 assertions, 0 failures."}}
{"id":"int-a6a6b2c8","kind":"field_change","created_at":"2026-08-14T04:08:42.718391Z","actor":"Yogthos","issue_id":"vf-mxm","extra":{"field":"status","new_value":"in_progress","old_value":"open"}}
{"id":"int-998ca0e2","kind":"field_change","created_at":"2026-08-14T10:08:05.13314Z","actor":"Yogthos","issue_id":"vf-mxm","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Fixed, deployed and confirmed live: 3 no-calls since deploy, all 3 recovered on the next turn opening inside the fence. Baseline was 41% of no-calls followed by another no-call."}}
{"id":"int-f9c0a63c","kind":"field_change","created_at":"2026-08-14T10:08:05.39104Z","actor":"Yogthos","issue_id":"vf-3k4","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed, deployed and confirmed live: 3 no-calls since deploy, all 3 recovered on the next turn opening inside the fence. Baseline was 41% of no-calls followed by another no-call."}}
{"id":"int-2c974dd2","kind":"field_change","created_at":"2026-08-14T21:30:30.02714Z","actor":"Yogthos","issue_id":"vf-met","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed and deployed mid-run: refuse only a same-engine repeat. review has no engine kind and is never touched; smt-on-lean runs and counts as agreement. Confirmed live in gen-23 — 3 refusals after the deploy, all on verify_lean/verify_smt, none on review."}}
{"id":"int-d3f65bd1","kind":"field_change","created_at":"2026-08-14T21:30:31.104873Z","actor":"Yogthos","issue_id":"vf-7k8","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Nine error-class hints shipped and deployed into gen-24, ranked by turns lost across gen-22/23/24. Unmatched errors deliberately get no hint."}}
{"id":"int-a6108cdd","kind":"field_change","created_at":"2026-08-14T21:30:31.789765Z","actor":"Yogthos","issue_id":"vf-cak","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Judge-gated same-claim check shipped; scoping bug found and fixed in vf-met. Field-proven in gen-23: 8 refusals, 15 comparisons, reasoning on hypotheses and conclusions rather than wording."}}
{"id":"int-39cae034","kind":"field_change","created_at":"2026-08-14T21:30:32.478702Z","actor":"Yogthos","issue_id":"vf-tso","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Duplicate of vf-mxm, fixed and field-proven: consecutive no-calls fell from 41% to 22%."}}
{"id":"int-f4e08cea","kind":"field_change","created_at":"2026-08-14T21:38:01.118219Z","actor":"Yogthos","issue_id":"vf-9bo","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed and deployed. safe-state settles on retract_rule/thesis/done/give_up/review; a coverage test now walks the gate table so a gate without a settle rule fails in tests."}}
{"id":"int-8a78af6b","kind":"field_change","created_at":"2026-08-14T21:48:34.042587Z","actor":"Yogthos","issue_id":"vf-v6x","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed and deployed mid-run. Argument-shape faults are :mechanics; the count still bounds a branch looping on bad calls but no longer reads as a failed line of inquiry. Also covers vague-claim, which is refused before any engine runs."}}
{"id":"int-0992c8ba","kind":"field_change","created_at":"2026-08-15T00:00:29.132885Z","actor":"Yogthos","issue_id":"vf-f5c","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed and deployed mid-run. 40% relevance floor; below it the names still show with the overlap count and the advice to prove it directly."}}
{"id":"int-03040855","kind":"field_change","created_at":"2026-08-15T00:03:09.771027Z","actor":"Yogthos","issue_id":"vf-0qf","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"gen-21 finished long ago; superseded by gen-22/23/24, documented in docs/phase-unwrapping-3.md and docs/harness-findings-gen22-24.md."}}
{"id":"int-184866ae","kind":"field_change","created_at":"2026-08-15T03:37:54.509753Z","actor":"Yogthos","issue_id":"vf-cfp","extra":{"field":"status","new_value":"in_progress","old_value":"open"}}
{"id":"int-e8c1c4f1","kind":"field_change","created_at":"2026-08-15T03:56:41.671235Z","actor":"Yogthos","issue_id":"vf-cfp","extra":{"field":"status","new_value":"open","old_value":"in_progress"}}
{"id":"int-846088e1","kind":"field_change","created_at":"2026-08-15T05:22:55.403667Z","actor":"Yogthos","issue_id":"vf-3yv","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed and deployed mid-run: sessions are checked for liveness before reuse, and a dead REPL is neutral wherever it surfaces rather than only on the throw path. gen-27 is the first run with it live."}}
{"id":"int-b518b272","kind":"field_change","created_at":"2026-08-15T05:53:10.157759Z","actor":"Yogthos","issue_id":"vf-4yb","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed and deployed mid-run. Matches weighted by IDF computed from the index in one memoised pass; the relevance floor is now a share of the query's IDF mass. Verified on the gen-27 query that prompted it — correctly reported as no good match — while a genuine query still returns its matches."}}
{"id":"int-af6b710b","kind":"field_change","created_at":"2026-08-15T06:31:51.9949Z","actor":"Yogthos","issue_id":"vf-wgo","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed and deployed mid-run: eight consecutive searches without an engine attempt refuse the ninth, and any attempt clears the count including a failing one."}}
{"id":"int-d067088b","kind":"field_change","created_at":"2026-08-15T08:20:58.809633Z","actor":"Yogthos","issue_id":"vf-64d","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed and deployed mid-run: the lint refuses an anonymous example when no named declaration sits alongside, naming theorem/lemma as the fix. Caught before Lean runs, same as the sorry check."}}
{"id":"int-b4d79ce8","kind":"field_change","created_at":"2026-08-15T11:52:21.599107Z","actor":"Yogthos","issue_id":"vf-k1p","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed and deployed mid-run: an identical encoding from the same engine is settled before the judge is consulted. Whitespace-insensitive and nothing looser, so it cannot false-positive."}}
{"id":"int-6501f9e8","kind":"field_change","created_at":"2026-08-15T13:50:33.920822Z","actor":"Yogthos","issue_id":"vf-15u","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed and deployed mid-run: when the REPL returns {:message ...} the message reaches the branch instead of a bare 'cannot tell'. Also confirms vf-4tw's value — 19 replies in gen-27 alone that the old code would have recorded as closed proofs."}}
{"id":"int-d6a6c58f","kind":"field_change","created_at":"2026-08-15T16:09:47.648728Z","actor":"Yogthos","issue_id":"vf-k0v","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed and deployed mid-run: :ok now requires an affirmative :env rather than the absence of error messages, and the REPL's rejection message reaches the caller."}}
{"id":"int-ab0c439a","kind":"field_change","created_at":"2026-08-15T16:09:49.357715Z","actor":"Yogthos","issue_id":"vf-4tw","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed and field-validated: apply-tactic requires an affirmatively empty goal list. gen-27 hit the ambiguous reply 19 times, every one of which the old code would have recorded as a closed proof. The sibling defect in run-command is vf-k0v, now also fixed."}}
{"id":"int-894a7204","kind":"field_change","created_at":"2026-08-15T16:09:50.420335Z","actor":"Yogthos","issue_id":"vf-cfp","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed and empirically verified. gen-27 started at 05:16 UTC, after the session registry landed at 05:02, created sessions, completed, and left none behind — every live repl belongs to gen-28, and the sole survivor predates the registry. Root cause was not jolt's destroy-tree (see docs/jolt-bugs.md) but session handles living only in the branch map, which three error paths discard."}}
{"id":"int-b61f648c","kind":"field_change","created_at":"2026-08-15T18:00:12.232919Z","actor":"Yogthos","issue_id":"vf-e8b","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Fixed and deployed: the Lean faithfulness note now asks whether the conclusion is the hypotheses handed back, and whether every object the claim names appears in the statement. Found via gen-28's shipped a#818 and its a#826."}}
