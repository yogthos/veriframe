node_modules/
dist/
*.tsbuildinfo

# Local GGUF models — large, not for VCS
models/
*.gguf

# Lean build artifacts + multi-GB mathlib cache. The workspace's
# lakefile.toml + lean-toolchain are tracked; users regenerate
# .lake locally with `lake update`.
tools/lean-workspace/.lake/
tools/lean-workspace/build/
